import React, { ReactNode } from "react";
type ThemeWrapperProps = {
    children: ReactNode;
};
declare function ThemeWrapper({ children }: ThemeWrapperProps): React.JSX.Element;
export default ThemeWrapper;
