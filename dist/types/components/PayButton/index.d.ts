import React from "react";
declare function PayButton({ amount, receipient, buttonClassname, }: {
    amount: number;
    receipient: string;
    buttonClassname?: string;
}): React.JSX.Element;
export default PayButton;
